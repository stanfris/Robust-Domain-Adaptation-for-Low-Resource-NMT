name: nlp2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - transformers==4.49
      - datasets
      - evaluate
      - torch
      - sacremoses
      - sacrebleu
      - unbabel-comet
      - accelerate>=0.26.0